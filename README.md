# parsing_speakers_kant
Автоматический поиск спикеров для лектория КАНТ

## Запуск локально
```bash
pip install -r requirements.txt
python database/init_db.py
python main.py

### **Шаг 2: GitHub репозиторий (5 мин)**

```powershell
# Инициализация git
git init
git branch -M main

# Добавляем файлы
git add .

# Первый коммит
git commit -m "feat: initial project structure for Kant speakers parser"

# Создаём репозиторий на GitHub (через браузер или gh CLI)
# В браузере: github.com/new → parsing_speakers_kant → Create repository

# Подключаем remote
git remote add origin https://github.com/ВашUsername/parsing_speakers_kant.git

# Пушим
git push -u origin main
